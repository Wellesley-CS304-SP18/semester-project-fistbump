<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name=author content="Dana L. Hsiao and Scott D. Anderson">
    <meta name=description content="">
    <meta name=keywords content="">
    <title>Home - CS304 Flask Assignment</title>
    <link rel="stylesheet" type="text/css" href="hwk6.css">
  </head>

  <body>
    <div id="navigation">
      <ul>
        <li><a href="{{url_for('lookup')}}">Home</a>
        <li><a href="{{url_for('search')}}">Search by Title</a>
        <li><a href="{{url_for('select')}}">Select from Incomplete Movies</a>
        <li><a href="{{url_for('login')}}">Login</a>
        <li><a href="{{url_for('rateMovies')}}">Rate Movies</a>
      </ul>
    </div>
    <div id="content">

     {% for message in get_flashed_messages() %}
         {{message}}
     {% endfor %}

        <h1>WMDB Interactions</h1>

        {% block content %}
        {% endblock %}



    </div>
  </body>
</html>
